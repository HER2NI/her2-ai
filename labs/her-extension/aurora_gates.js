/*
 * Public stub — Labs demo only.
 * No scoring logic. No proprietary gate behavior.
 */
export function evaluateAuroraWindow() {
  return {
    kind: "AURORA",
    qualified: false,
    note: "Public demo build: gating disabled.",
  };
}
